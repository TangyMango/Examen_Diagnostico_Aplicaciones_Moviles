//9.	Crea una función llamada generateGreeting que tome como parámetro un nombre y 
//      devuelva un saludo personalizado.
var name = "Bryan";

function generateGreeting(name) {
    alert("Hola, " + name + "!");
}

generateGreeting(name);